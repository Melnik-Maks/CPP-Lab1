export interface GiftOrderItem {
  lineId: string;
  productId: string;
  title: string;
  categoryLabel: string;
  packagingTitle: string;
  packagingPrice: number;
  totalPrice: number;
  note: string;
}
